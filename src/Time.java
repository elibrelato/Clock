import java.time.LocalTime;
import java.util.Date;
import org.apache.commons.net.ntp.NTPUDPClient;

public class Time {
    public static String getTime(){
        String host = "mx.pool.ntp.org";
        NTPUDPClient client = new NTPUDPClient();


        return time;
    }
}
